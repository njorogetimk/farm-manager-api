Get the all cows of a given gender

---
  tags:
    - Dairy Farm
  
  parameters:
    - in: header
      name: Authorization
      required: true
    
    - in: path
      name: gender
      required: true
  
  responses:
    200:
      description: "Cows found"
    404:
      description: "The given gender not found"