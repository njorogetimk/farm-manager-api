Gets all the users in the system

---
  tags:
    - Authentication
  parameters:
    - in: header
      name: Authorization
      required: true
  
  responses:
    200:
      description: "Found all users"
    401:
      description: "A problem with the authorization header"
    
